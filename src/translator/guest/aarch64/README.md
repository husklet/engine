# AArch64 Linux guest frontend

Decode guest AArch64 and lower it to the private validated IR. Guest register layouts and Linux signal-frame
encoding belong here; host-CPU instructions and host-OS calls do not.
