# AArch64 host backend

Lower validated IR to AArch64 host code. Current direct emitters are migrated here incrementally and remain
behind a compatibility adapter until differential and performance gates prove each IR family.
